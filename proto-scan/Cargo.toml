[package]
name = "proto-scan"
version = "0.1.0"
edition = "2024"

[features]
derive = ["dep:proto-scan-derive"]

[dependencies]
assert_matches = "1.5.0"
derive_more = { version = "2.1.1", features = ["from", "into", "try_from"] }
either = "1.15.0"
num-traits = "0.2.19"
proto-scan-derive = { path = "../proto-scan-derive", optional = true }

[dev-dependencies]
hex-literal = "1.1.0"
